#!/usr/bin/env bash

BIN=node_modules/.bin

NODE_ENV=development $BIN/watchify --debug --verbose --extension .es6 \
  --require ./playground/panels.dev:panels.dev \
  --outfile playground/app.js index.es6 & \
sudo $BIN/babel-node ./.bin/server.es6 & \
wait
